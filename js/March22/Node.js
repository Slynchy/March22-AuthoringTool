/*
    No, not that Node.js
*/

function visNode(props) {
    var node = props;
    //{ id, label, title, level, content }

    return node;
}
    
visNode.prototype.testFunc = function(something) {
    
};